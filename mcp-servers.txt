# MCP 서버 설치 명령어
# API 키는 환경변수로 설정하세요

# User-level MCP servers
claude mcp add stitch-mcp -s user -- npx -y stitch-mcp

# Project-level MCP servers
claude mcp add hyperbrowser -s user -- npx --yes hyperbrowser-mcp
